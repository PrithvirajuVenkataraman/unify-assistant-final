{
    "name":  "unify-assistant",
    "private":  true,
    "version":  "1.0.0",
    "type":  "module",
    "scripts": {
        "lint": "node --check api/index.js && node --check api/chat-groq.js && node --check api/current-facts.js && node --check api/markets.js && node --check api/search.js && node --check api/speech.js && node --check api/vision.js",
        "check": "npm run lint"
    },
    "dependencies":  {},
    "vercel":  {
                   "functions":  {
                                     "api/**/*.js":  {
                                                         "maxDuration":  60
                                                     }
                                 },
                   "rewrites":  [
                    {
                        "source":  "/",
                        "destination":  "/index.html"
                    }
                ]
               }
}
